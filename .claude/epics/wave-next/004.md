---
name: Fix ruff errors + add logging to silent exceptions
status: open
created: 2026-04-01T02:13:00Z
updated: 2026-04-01T02:13:00Z
github: (will be set on sync)
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Fix ruff errors + add logging to silent exceptions

## Description

Fix the 3 ruff lint errors and add logger.warning() to critical silent `except Exception: pass` blocks in API routes.

## Acceptance Criteria
- [ ] `ruff check app/ tests/` = 0 errors
- [ ] buildings.py: search index failures logged with logger.warning
- [ ] diagnostics.py: search index failures logged
- [ ] documents.py: search failures logged
- [ ] All 7150+ backend tests still pass
- [ ] No new `except Exception: pass` introduced

## Technical Details

**Ruff fixes:**
- backend/tests/test_registry_connector_service.py: unsorted imports (I001), unused imports (F401)
- Fix: `cd backend && ruff check --fix app/ tests/`

**Exception logging (critical API routes):**
```python
# Before:
except Exception:
    pass

# After:
except Exception:
    logger.warning("Search index update failed for building %s", building_id, exc_info=True)
```

Target files: app/api/buildings.py, app/api/diagnostics.py, app/api/documents.py

## Effort Estimate
- Size: S
- Hours: 2
